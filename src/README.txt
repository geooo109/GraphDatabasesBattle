
Note:

the input file for the seeds

must have name:

is_[NUMBER].txt
ic_[NUMBER].txt
bi_[NUMBER].txt
mib_[NUMBER].txt


you can find the names of the parameters from the source code.

the seed file must be in the form of:

personId:
INPUT
EMPTY_LINE

for Neo4j in the strings you must add " " (tigergraph does not need them)
